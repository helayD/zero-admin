# MEMORY.md

## Codex 会话约定

- 用户说“codex 清理上下文”时：在当前 Codex 会话里执行 `/compact`。
- 用户说“codex 新会话”时：不要 `resume --last`，直接启动一个全新的 Codex 会话：`codex --dangerously-bypass-approvals-and-sandbox`。
- 用户说“codex 继续 / 恢复会话”时：优先执行 `codex resume --last`。

## 质量优先原则

- 对这个项目来说，避免 Codex 上下文污染比少开新会话更重要。
- 只要出现以下迹象，就优先考虑 `/compact` 或直接新开会话：
  - 对当前需求、分支、任务目标开始混淆
  - 反复引用过时计划或旧实现
  - 把别的项目/别的模块上下文带进来
  - 连续多轮修改后推理质量明显下降
- 一旦怀疑上下文被污染，宁可保守地新开会话，也不要硬接着做。

## Compact / 新会话后的恢复动作

- 先重新阅读仓库内的 `AGENTS.md`、`CONVENTIONS.md`、`prd-todo.md`（若存在）。
- 如项目内有会话持久化文件，也优先读取：`task_plan.md`、`findings.md`、`progress.md`。
- 先确认当前任务、未提交改动、最近一次明确指令，再继续编码。

## 工程约束

- 项目质量优先于速度。
- 禁止使用 `goctl` 指令；不要用它生成、覆盖或改写项目代码。
- 不要在上下文不干净时做架构决策、批量重构或删改核心逻辑。
- 对不确定的历史结论，先重新读文件/状态，不靠“感觉记得”。
- 重大改动前先收束任务边界，必要时把计划写到磁盘，而不是全靠会话上下文。
