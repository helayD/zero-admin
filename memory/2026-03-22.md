- 用户明确要求：在 zero-admin 仓库里，同类 BMAD `create-story` 文档必须以项目内最近同类 story（尤其 `1.6A` / `1.6B`）作为风格与展开粒度基线；不能只满足结构合规却出现明显篇幅和密度缩水，也不能用“阶段不同”来解释这种差异。
- 2026-03-22 已完成 Story 1.7《治理审计与安全事件检索》的 create-story 重写、dev-story 实现、增强和 review 修复，当前 story 文档状态已回填为 `review`，sprint-status 中 `1-7-治理审计与安全事件检索` 也已更新为 `review`。
- 2026-03-22 在修复 1.7 过程中发现并修补了 `.agents/skills/zero-admin-remote-deploy/scripts/deploy_remote.sh` 的兼容性问题：macOS bash 3 + `set -u` 下空数组 `SELECTED_SERVICES[@]` 会触发 unbound variable，需要使用 `${SELECTED_SERVICES[@]-}` 兼容展开。
- 2026-03-22 远程部署到 `47.107.224.56` 的 GitHub-backed git sync 两次失败，根因不是代码编译，而是远端服务器访问 `https://github.com/helayD/zero-admin.git` 的 443 TLS/网络问题（一次 GnuTLS recv error，一次 connection timed out）。后续远程部署若再次失败，优先检查远端到 GitHub 的网络连通性，而不是先怀疑本地构建结果。
